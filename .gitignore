# Compiled Object files, Static and Dynamic libs (Shared Objects)
*.o
*.a
*.so

# Folders
_obj
_test

# Architecture specific extensions/prefixes
*.[568vq]
[568vq].out

*.cgo1.go
*.cgo2.c
_cgo_defun.c
_cgo_gotypes.go
_cgo_export.*

_testmain.go

*.exe
.idea

# Go workspace files are local-only dev aids and must not be committed.
# Note: they are NOT required here — each submodule's go.mod carries a local
# `replace` for its in-repo core dependency, which is what `go mod tidy` honors.
go.work
go.work.sum
